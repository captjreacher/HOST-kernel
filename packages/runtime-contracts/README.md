# @host/runtime-contracts

Transport-neutral runtime contracts for HOST.

This package defines the shared application-facing contracts for:

- authentication context
- correlation and tracing context
- request context propagation
- logger, metrics, and tracer interfaces

It does not implement authentication, authorization, logging vendors, metrics backends, tracing SDKs, or framework integrations.
